# Set `conda-like` environment prefix to the `PATH` environment variables

**\[deprecated\]** Set `conda-like` environment prefix to the `PATH`
environment variables.

## Usage

``` r
cmd_conda(...)
```

## Arguments

- ...:

  Additional arguments passed to
  [`cmd_condaenv()`](https://wanglabcsu.github.io/blit/reference/cmd_wd.md).
